# Hint

Good — if you rebased and landed here clean, you're caught up on the
bearing. That's as far as this particular fix goes for a cipher-side
crew. Next: a trick that never made the main journal is sitting on its
own branch somewhere — `git branch -a` will turn it up. Bring it home
with a cherry-pick.
